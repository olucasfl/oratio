VITE_API_URL=
# Client ID do cliente OAuth "Web application" (Google Cloud Console).
# Mesmo valor do GOOGLE_CLIENT_ID do backend. Sem isto, o botão "Entrar com
# Google" simplesmente não aparece — o login por e-mail/senha segue normal.
VITE_GOOGLE_CLIENT_ID=
